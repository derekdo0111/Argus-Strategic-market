# 2026-06-21 工作日志

## v0.7.1 — 全链路8项同步修复

### 背景
v0.7.0 CQ 8维度上线后，审计发现文档/代码有 8 项未同步：
- P0: CONTEXT.md 和 coordinator.md 仍写"5维度"
- P1: A7应付周转用 total_liab 占位、A4 dim7 不传逐年比率、validate 不警告新字段缺失、fin_exp 负值反哺 PR
- P2: industry_profiles.yaml 缺少 CQ 行业阈值

### 改动
| 文件 | 改动 |
|------|------|
| CONTEXT.md | "5维度"→"8维度" ×2, v0.7.0→v0.7.1 |
| coordinator.md | "CQ 5维度表"→"CQ 8维度表" |
| data_summarizer.py | A1: +accounts_payable/notes_payable 字段; A4 dim7: +yearly_ratios; A7: 应付周转真实计算 + cash_conversion_cycle |
| coordinator.py | _validate_raw_data() +7字段缺失 WARNING |
| penetration_return.py | fin_exp = max(0, fin_exp) |
| industry_profiles.yaml | 6行业+默认 cq_thresholds.supplier_debt_ratio_max |
| config.py | v0.7.0→v0.7.1 |
| pyproject.toml | v0.7.0→v0.7.1 |
| CHANGELOG.md | +v0.7.1 条目 |
| MEMORY.md | 版本号 + 规则文件数更新 |

### 测试
✅ 86/86 全部通过

---

## v0.7.3 — R4 重大事件与资本运作 (QRV v4 框架)

### 背景
用户指出：分众传媒3月定增收购新潮传媒(83.5亿)这样的大事，搜索/提取/分析三层全漏掉。

### 设计
三层联动，不新增搜索成本（挂在现有 q_websearch 多1条关键词）：
- **搜索层**: q_websearch +4th keyword "定增 并购 收购 资产重组 资本运作 重大合同 重大事项"
- **提取层**: WebSearchExtractor +`_extract_corporate_events()` (5类事件正则: 定增/并购/重组/重大合同/诉讼)
- **分析层**: QRV prompt +R4 模块 (事件清单表 + 影响分析 + 跨维度联动 Q2/Q3/R3/V1)

### 改动文件
| 文件 | 改动 |
|------|------|
| turtle_qrv.yaml | v3→v4: +1搜索关键词, +R4分析模块, +R4 prompt, +R4打分卡行, +extracted_facts板块 |
| websearch_extractor.py | +`_extract_corporate_events()` (~150行) + `_extract_date()` + `_parse_amount()` |
| data_summarizer.py | data_sufficiency +R4_corporate_events |
| turtle-coordinator.md | Step 7/7.5/8: 11模块同步 + 第8类提取器 |
| CONTEXT.md | +R4模块 + WebSearchExtractor描述更新 |
| config.py | v0.7.1→v0.7.3 |
| pyproject.toml | v0.7.1→v0.7.3 |
| package.json | v0.7.0→v0.7.3 |
| test_websearch_extractor.py | **新建** 6个corporate_events测试 |
| test_spec_compliance.py | +6个R4合规测试 (prompt含R4/rules有r4模块/data_sufficiency/scorecard/extracted_facts) |
| CHANGELOG.md | +v0.7.3完整条目 |

### 测试
✅ 98/98 全部通过 (原86 + 新增12)

---

## v0.7.4 — 报告输出质量四件套 (Prompt + 数据锚点)

### 背景
用户反馈四个问题：
1. 报告结构不稳定 — 开头一会儿"质量分析"一会儿"整体研判"/"分析摘要"，导航栏缺段
2. 超链接失效 — [W-q-2] 无 URL、[A7] 参考来源中消失
3. 表格趋势 up/down/stable 不直观
4. 估值区间建议两次输出差距极大

### 改动
| 文件 | 改动 |
|------|------|
| turtle_qrv.yaml | +强制输出结构声明 + 箭头替英文 + 估值公式约束(锚定A8分位) + A系列参考来源 |
| data_summarizer.py | reference_index +A1-A8 数据锚点 (~+90行) |
| turtle-coordinator.md | Step 8 标注 v0.7.4 输出质量 |
| CONTEXT.md | v0.7.3→v0.7.4 + DataSummarizer v2.1 |
| config.py | v0.7.3→v0.7.4 |
| pyproject.toml | v0.7.3→v0.7.4 |
| package.json | v0.7.3→v0.7.4 |
| CHANGELOG.md | +v0.7.4 完整条目 |

### 测试
✅ 98/98 全部通过 (纯 Prompt + 数据索引层改动，零计算逻辑变更)

---

## v0.7.5 — 参考来源锚点跳转 + 置信度使用规则

### 背景
用户反馈两个设计缺口：
1. 报告引用标记 `[W-q-3]` 是纯文本，无法点击跳转到参考来源
2. websearch 有置信度数据（HIGH/MEDIUM/LOW/NONE），但 Prompt 没有告诉 LLM 如何使用

### 设计
三层联动，零计算逻辑变更：
- **锚点跳转 (turtle_qrv.yaml)**: 核心铁律第2条改为 Markdown 锚点格式；W/A-引用表模板加 `<a id>` + 可点击链接
- **置信度规则 (turtle_qrv.yaml)**: 新增「置信度使用规则」整节，四级定义 + 降权要求 + W-引用表加置信度列
- **数据层 (data_summarizer.py)**: W条目注入 module_confidence + snippet_confidence；新增 confidence_summary 汇总

### 改动文件
| 文件 | 改动 |
|------|------|
| turtle_qrv.yaml | v4.1→v4.2: +锚点跳转指令 +置信度使用规则整节 +W/A引用表模板加锚点+置信度列 (~+40行) |
| data_summarizer.py | v2.1→v2.2: W条目加置信度字段 + confidence_summary (~+25行) |
| turtle-coordinator.md | v0.7.4→v0.7.5 + Step 7 置信度说明 + Step 8 输出质量 |
| CONTEXT.md | 版本号 + DataSummarizer v2.2 |
| config.py | v0.7.4→v0.7.5 |
| pyproject.toml | v0.7.4→v0.7.5 |
| package.json | v0.7.4→v0.7.5 |
| CHANGELOG.md | +v0.7.5 完整条目 |
| MEMORY.md | 版本号 + v0.7.5条目 + v0.7.4条目 |

### 测试
✅ 98/98 全部通过 (纯 Prompt + 数据索引层改动，零计算逻辑变更)
